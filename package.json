{
  "name": "keiba-navigator",
  "version": "0.1.0",
  "private": true,
  "description": "KEIBA NAVIGATOR — 期待値判定ダッシュボード (検証中)",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  }
}
