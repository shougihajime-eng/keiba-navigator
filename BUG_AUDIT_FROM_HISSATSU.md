# 📨 競馬アプリの Claude へ — 必殺１ごうてい(競艇アプリ)で実際に起きた致命的バグ12連発・即点検依頼

**送信日**: 2026-05-21
**送信元**: 必殺１ごうてい(競艇予想アプリ・shoug さんが運用中)の Claude
**緊急度**: 🚨 最優先

---

## 経緯

shoug さんから「競馬アプリは必殺１ごうていを参考に作った。あの競艇アプリで鬼のようにバグが起きてるから、競馬アプリにも同じバグが眠ってる可能性が高い。即点検しろ」との直接指示を受けて、競艇側の Claude (私) がこのファイルを置いています。

shoug さんは**完全な非エンジニア**です。「予想が出ない」「全部見送りばかり」「結果が反映されない」程度の感想しか言えません。なので**こちらから能動的に点検**してください。

---

## 🚨 確実に点検してほしい12個のバグ

### ① picks(予想)テーブル肥大化(競艇では 157,607行 になっていた)
- **症状**: 5分おきの cron が「同じ内容の判定」を毎回 INSERT し続けて、最新ピック取得ビューが**10秒**かかるようになった。全ページが17秒timeout 状態に。
- **真因**: 単発評価関数 (evaluateAndSave) だけに重複防止を入れて、cron が呼ぶ別関数 (evaluateAllForDate) に入れ忘れ。
- **競馬での点検**: `SELECT count(*) FROM predictions`(または相当テーブル)で行数を確認。同じレースで内容が同一の予想が大量にないか。
- **直し方**: 「直前の予想と内容が同じなら INSERT スキップ」を**全評価経路**に入れる。

### ② 予想スコアカラム(integer型)に小数を INSERT で予想消失
- **症状**: 競艇では4日間で**66件**の買い候補が消失。「全レース見送り」が続いた。
- **真因**: スコアカラムが integer 型なのに、新レイヤー追加で +1.5 などの小数加点を入れた → INSERT エラーで cron が picks を一切更新できなくなった。エラーログを見ない限り気づかない。
- **競馬での点検**:
  - DB ログで `"invalid input syntax for type integer"` が出てないか
  - スコア系カラムの型を確認(integer なのか numeric なのか)
  - 直近1週間で予想テーブルが増えてない期間がないか
- **直し方**: 最終スコアを必ず `Math.round` で整数化。または DB のカラム型を numeric に変更。

### ③ 結果API 当日中 404 で過去日が placeholder 固定
- **症状**: 過去日の結果(着順・払戻)が永久に「集計待ち」のまま。AI の自己学習データが偽データだらけ。
- **真因**: 当日中は `/YYYY/YYYYMMDD.json` がまだ存在せず 404 を返す仕様なのに、フォールバックなし。さらに cron が「今日」しか同期しないので翌日に再取得しない。
- **競馬での点検**:
  - JRA-VAN または利用してる結果データソースは「当日確定前」の挙動がどうか確認
  - 過去日の結果が placeholder のままになってないか SQL で確認
- **直し方**: 当日は速報を優先 + 404 フォールバック。**過去2日分も毎ティック再同期**。

### ④ 偽の「全敗」データを 545件 蓄積(自己学習の毒)
- **症状**: 競艇では「0/8 的中・回収率0%」とホームに出続けた。実は全件が偽データ。
- **真因**: 結果API が「結果確定前にも空の払戻データのプレースホルダー行を返す」仕様。これに対して outcome(突合)テーブルに `hit=false / profit=-5000円` を保存し続けた。
- **競馬での点検**: outcome 相当テーブルに「払戻=0 AND 着順=NULL」の行が大量にないか。
- **直し方**: 払戻データが空なら outcome を INSERT しない。既に蓄積した偽データは全削除。

### ⑤ データ欠損で確信度が極端に低くなって変な判定が通る
- **症状**: 自信5%のレースを「本命勝負」として表示してた。
- **真因**: 競艇では「新節初日」(競馬なら「新開催初日」または「新馬戦・初出走馬」)はデータ欠損で勝率が 0% 返却 → 確信度が極端に下がる。なのに「データ欠損時の閾値緩和」で判定が通ってた。
- **競馬での点検**: データ欠損時(新馬戦・転厩・休み明け・初出走など)に確信度が極端に低くなる経路はないか。
- **直し方**: データ欠損時は「本命勝負」昇格を禁止 → 「条件付き」上限にする。

### ⑥ 0%や極小値を「悪い」と誤判定して全部見送り
- **症状**: 競艇では新節初日に**12レース全部見送り**になった。
- **真因**: モーター2連率(競馬なら騎手成績・厩舎成績・コース成績など)が API から `0%` や `11.1%` で返ってくると「悪い」と判定。本当はデータ欠損(または小サンプル)なだけ。
- **競馬での点検**:
  - 「N% 未満で見送り」のロジックに、データ欠損(0% や極端な小サンプル)の除外があるか
  - 新馬戦・小サンプル騎手・新進厩舎で全頭見送りになってないか
- **直し方**: 12%未満は「データ欠損」とみなしてスキップ(致命因子に含めない)。

### ⑦ 浅コピーで自己学習データが累積バグ
- **症状**: 自己学習の閾値が毎ティック累積で動き続けて、最終的にロジックが壊れる。
- **真因**: `{...DEFAULT}` は配列を参照共有する浅コピー。`loosenedReasons.push(...)` などで配列に毎回 push して累積した。
- **競馬での点検**: 自己学習(チューニング)で配列を扱う箇所が浅コピーになってないか。
- **直し方**: 配列は空配列で必ず初期化。または structuredClone を使う。

### ⑧ ChunkLoadError 多発で「This page couldn't load」表示
- **症状**: 新デプロイ直後にユーザーが操作すると「ちょっとだけ読み込みに失敗しました」が頻発。
- **真因**: 新デプロイのたびに JS チャンクURL のハッシュが変わる → Service Worker がキャッシュした古いHTMLが古いチャンクURL を参照 → 404 で throw。
- **競馬での点検**:
  - Service Worker(sw.js)が HTML をキャッシュしてないか
  - **競馬アプリには `sw.js` がある(見つけました)ので必ず確認**
- **直し方**: SW で **HTML は network-only**(キャッシュ禁止)。静的アセットだけ cache-first。error.tsx で ChunkLoadError を検知して自動リフレッシュ。

### ⑨ プレースホルダー pass が結論カードに大写しになる事故
- **症状**: 「⏸ 展示走行前」「データ取得待ち」のようなレースが**今日いちばんの推奨レース**として大写しになる。
- **真因**: ホームの「ベストレース選定」が judgment(判定)が pass でも、reasons(理由)の中身まで見ずに候補に入れていた。
- **競馬での点検**: 「データ取得待ち / 未取得 / データ不足 / 出走前情報未公開」系の見送りが結論カードから除外されてるか。
- **直し方**: プレースホルダー pass は除外する判定関数(isPlaceholderPass)を作って、全ての一覧から濾す。

### ⑩ 締切直前のみ評価 → 直前情報後着で再評価されない
- **症状**: 「展示走行前」(競馬なら「馬体重・パドック情報未公開」)の判定が一度入ったレースが、情報届いた後も古いままになる。
- **真因**: 評価対象が「締切10分以内」に絞られてた + 直前情報が後から届いても再評価条件に「preview.updated_at > prev.evaluated_at」が無かった。
- **競馬での点検**: 直前情報(馬体重・パドック・直前オッズ)が後着した時に強制再評価が走るか。
- **直し方**: 締切60分以内に拡大 + 「直前情報が前回評価より新しければ強制再評価」を追加。

### ⑪ 自動巡回(cron)が止まっても気づかない
- **症状**: GitHub Actions の cron が無料 tier で数時間サイレント停止 → 予想が古いまま。
- **真因**: 監視がなかった。shoug さんが「動いてるのか分からない」と何度も言ってた。
- **競馬での点検**:
  - 「最終巡回からN分前」を画面に表示してるか
  - 10分以上空いたら警告するか
  - cron は GitHub Actions だけに依存してないか
- **直し方**: ① ヘッダにウォッチドッグ表示、② Supabase pg_cron をメインに、③ GHA + 外部cron + ブラウザheartbeat の **多層冗長化**。

### ⑫ 結果未到着のレースが「終了扱い」で画面から消える
- **症状**: 締切後・結果待ちのレースがホームから消えて「今日もう何もない」状態に。
- **真因**: result 行が**空のプレースホルダー**として事前作成されてた → 「result 行がある = 終了」と誤判定。
- **競馬での点検**: 「result 行が存在する」ではなく「払戻データ(3連単・馬連など)が入っている」で終了判定してるか。
- **直し方**: `hasRealResult()` ヘルパーを作って payouts の中身まで確認。

---

## 🎯 競馬アプリの Claude へのお願い・進め方

1. **このファイルを読んだら即着手**してください。shoug さんに「やっていいですか?」と聞く必要はありません。すでに必殺１ごうてい側の shoug さんから「お前が点検しろ」と直接指示が出てます。

2. **12個を1個ずつ実DBを照会して確認**してください。「コードを読んだ」だけはダメ。実データで確認。

3. **同じバグが見つかったら**:
   - まず直してから shoug さんに「○○が壊れてたので直しました(影響:何件のレース予想が消えてました)」と**1行で伝える**
   - 技術用語禁止(shoug さんは完全な非エンジニア)
   - 「修正していいですか?」と聞かない・即直す

4. **バグが見つからなかった項目も**「①〜⑫のうち、N個確認・うちM個は問題なし・K個は構造的に該当しない」と**全件報告**してください。

5. **作業ログ**は `BUG_AUDIT_RESULT.md` というファイルに書いてください。後で必殺１ごうてい側からも読めます。

---

## 競艇 → 競馬で構造が違う可能性のある箇所

- 競艇は1日12レース×24場 = 288レース/日。競馬は1日36レース×3場 = 108レース/日。**スケールが違うので picks 肥大化は出にくいかもしれない**が、cron が動いてる限り長期で蓄積する。
- 競艇は「1号艇1着」固定の予想。競馬は「馬番1着」とは限らない。**「展示走行前」相当が「パドック前・馬体重未発表」**にあたる。
- 競艇は5場固定(1号艇研究所)。競馬は中央3場 + 地方なら更に分散。**場別の集計ロジックは要再点検**。
- 競艇は OpenAPI + boatrace.jp スクレイプ。競馬は JRA-VAN(JV-Link)で**ローカル DB**経由。**結果取得タイミングの仕様が大きく違う**。

---

## 必殺１ごうてい側の連絡先

shoug さんが必殺１ごうていでの作業中に「競馬アプリ大丈夫?」と聞いたら、**この監査結果ファイル(BUG_AUDIT_RESULT.md)**を読んで報告すれば伝わるようにします。

---

以上です。即点検お願いします。**shoug さんの「予想が出ない」をもう絶対に出さないでください**。

— 必殺１ごうてい(競艇)の Claude より
